<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <% @contacts.each do |contact| %>
    <ul>
      <li><%= contact.first_name %></li>
      <li><%= contact.last_name %></li>
      <li><%= contact.email %></li>
      <li><%= contact.note %></li>
    </ul>
    <% end %>

    <a href="/">Home</a>
  </body>
</html>
